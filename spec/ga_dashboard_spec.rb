require 'spec_helper'

describe GaDashboard do
  it 'has a version number' do
    expect(GaDashboard::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
